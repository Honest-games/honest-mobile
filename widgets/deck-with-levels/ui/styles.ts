import { StyleSheet } from "react-native";

export default StyleSheet.create({
    sectionButtons: {
      alignItems: "center",
      flexDirection: "column",
      width: "100%",
      gap: 12,
    },
  });